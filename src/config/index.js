const REQUEST_URL = 'https://jsonplaceholder.typicode.com'
export const GET_USERS = `${REQUEST_URL}/users`
export const GET_POSTS = `${REQUEST_URL}/posts`
export const GET_COMMENTS = `${REQUEST_URL}/comments`
